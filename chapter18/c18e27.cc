// (a) All the members in MI and its base classes members which are not
// private. And the global variables.

// (b) dval, ival

// (c)
dval = Base1::dval + Derived::davl;

// (d)
fval += dvec.back();

// (e)
Derived::sval.front() += Base1::cval;
